# ResNet U-Net RGB-only baseline for segmentation
exp_name: "seg_rgb_baseline"

# Task
task: "segmentation"  # 'classification' or 'segmentation'

# Data
data_root: "MM5_SEG"
train_split: "train"
eval_split: "eval"
modalities: ["rgb"]
img_size: [224, 224]

# Model
model_type: "unet_rgb"  # 'unet_rgb', 'unet_early_fusion', 'unet_mid_fusion'
pretrained: true

# Training
batch_size: 16  
num_workers: 4
epochs: 100
learning_rate: 0.001
weight_decay: 0.0001

# Saving
save_dir: "experiments/seg_rgb_merged"
save_freq: 50